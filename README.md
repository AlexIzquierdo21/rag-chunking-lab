# rag-chunking-lab
Compare RAG chunking strategies (fixed, recursive, semantic, sentence-window, late chunking) on any document corpus. Runs 100% locally with Ollama + sentence-transformers. No API keys required.
